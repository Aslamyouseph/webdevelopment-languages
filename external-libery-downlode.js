import { swapCase } from "swap-case"; // in this way we are imorting the file from the website

const result = swapCase("aslam youseph"); // converted aslam youseph into cappital letter
console.log(result);

/* first we need to intall the npm package on this folder/project (only one time is needed to install the npm package in a project)
npm init  => command is used to install the npm package 

After the downlode npm package then package.json file will occused. then inside this file there is a dependensy object contain 
if we look this dependency then we can see all the downloded liberary on this


https://www.npmjs.com/   -- this is the site which we need to download the npm library 


*/
